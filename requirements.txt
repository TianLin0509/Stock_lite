# A股智能投研助手 v3 依赖清单
# 安装：pip install -r requirements.txt

streamlit>=1.36.0     # 需要 st.write_stream + st.container(border=True)
openai>=1.30.0        # 统一调用 Qwen / 智谱 / 豆包 / DeepSeek（OpenAI兼容接口）
tushare>=1.4.0
akshare>=1.14.0       # Tushare 不可用时的备用数据源
numpy>=1.24.0
pandas>=2.0.0
stumpy>=1.13.0        # Matrix Profile / MASS 子序列形状匹配（可选但推荐）
plotly>=5.18.0
requests>=2.31.0
alibabacloud_bssopenapi20171214>=5.1.1
alibabacloud_tea_openapi>=0.4.3
volcengine>=1.0.218
